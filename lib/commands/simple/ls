#!/bin/bash

main() {
  local -r channel="$2"

  {
    printf "Available Commands:\n"
    find "$botLib/commands/simple" -type f -printf "%f\n" \
      | sort \
      | tr "\n" " "
  } | privmsg "$channel"
}

main "$@"
